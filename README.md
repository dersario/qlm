# QuickLead Manager (QLM)

Централизованная система для приема, обработки, распределения и учета заявок с сайтов и лендингов.

## 🚀 Возможности

- **Прием заявок**: REST API для приема заявок с сайтов и лендингов
- **Управление проектами**: Настройка проектов с уникальными API ключами
- **Ролевая модель**: Администраторы и операторы с разными правами доступа
- **Вебхуки**: Асинхронная отправка заявок в внешние системы
- **Фильтрация и поиск**: Удобная работа с большим количеством заявок
- **Статистика**: Дашборд с аналитикой по заявкам
- **Уведомления**: Интеграция с Telegram и Slack
- **Валидация**: Проверка телефонов и email адресов

## 🛠 Технологии

- **Backend**: FastAPI (Python 3.13)
- **База данных**: SQLite + SQLAlchemy
- **Аутентификация**: JWT токены
- **Асинхронные задачи**: Celery + Redis
- **Управление зависимостями**: Poetry

## 📋 Установка и запуск

### 1. Установка зависимостей

```bash
# Установка Poetry (если не установлен)
curl -sSL https://install.python-poetry.org | python3 -

# Установка зависимостей проекта
poetry install
```

### 2. Инициализация базы данных

```bash
# Активация виртуального окружения и запуск инициализации
poetry run python init_db.py
```

### 3. Запуск приложения

```bash
# Запуск через Poetry скрипт (рекомендуется)
poetry run start

# Или напрямую через uvicorn
poetry run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 4. Запуск Celery (для вебхуков)

```bash
# В отдельном терминале
poetry run celery -A app.celery_app worker --loglevel=info
```

## 🔑 Первоначальная настройка

После запуска `init_db.py` создается администратор:

- **Email**: admin@quicklead.local
- **Username**: admin
- **Password**: admin123

⚠️ **Обязательно смените пароль после первого входа!**

## 📡 API Эндпоинты

### Внешний API (для приема заявок)

```bash
# Прием заявки
POST /api/v1/lead
Headers: X-API-Key: <project_api_key>
Content-Type: application/json

{
  "name": "Иван Иванов",
  "phone": "+7 (999) 123-45-67",
  "email": "ivan@example.com",
  "message": "Интересует ваша услуга",
  "utm_source": "google",
  "utm_campaign": "summer_sale",
  "custom_fields": {
    "budget": "100000",
    "timeline": "asap"
  }
}
```

### Внутренний API (для управления)

```bash
# Аутентификация
POST /api/auth/login
{
  "username": "admin",
  "password": "admin123"
}

# Получение заявок
GET /api/leads
Headers: Authorization: Bearer <jwt_token>

# Создание проекта
POST /api/projects
Headers: Authorization: Bearer <jwt_token>
{
  "name": "Новый проект",
  "description": "Описание проекта",
  "webhook_url": "https://example.com/webhook"
}
```

## 🔧 Конфигурация

Настройки приложения находятся в `app/config.py`:

```python
# Основные настройки
SECRET_KEY = "your-secret-key-change-in-production"
DATABASE_URL = "sqlite:///./quicklead.db"

# JWT настройки
ACCESS_TOKEN_EXPIRE_MINUTES = 30

# Redis для Celery
REDIS_URL = "redis://localhost:6379"

# Telegram (опционально)
TELEGRAM_BOT_TOKEN = "your-bot-token"
TELEGRAM_CHAT_ID = "your-chat-id"
```

## 🎯 Роли пользователей

### Администратор
- Полный доступ ко всем функциям
- Управление проектами и пользователями
- Просмотр всех заявок
- Настройка системы

### Оператор
- Доступ только к назначенным проектам
- Работа с заявками (просмотр, редактирование, комментарии)
- Ограниченный доступ к настройкам

## 📊 Вебхуки

Система поддерживает асинхронную отправку заявок в внешние системы через вебхуки:

```python
# Пример payload вебхука
{
  "id": 123,
  "project_id": 1,
  "name": "Иван Иванов",
  "phone": "+79991234567",
  "email": "ivan@example.com",
  "message": "Интересует услуга",
  "status": "new",
  "priority": 1,
  "utm": {
    "source": "google",
    "medium": "cpc",
    "campaign": "summer_sale"
  },
  "custom_fields": {...},
  "created_at": "2024-01-15T10:30:00Z"
}
```

## 🔔 Уведомления

### Telegram
1. Создайте бота через @BotFather
2. Получите токен и chat_id
3. Укажите в настройках приложения

### Slack
1. Создайте Slack App
2. Получите Bot Token
3. Укажите токен и канал в настройках

## 📈 Мониторинг

- **Логи вебхуков**: Все попытки отправки вебхуков логируются
- **Статистика**: Дашборд с количеством заявок по статусам
- **Метрики**: Конверсия, заявки за период

## 🛡 Безопасность

- Шифрование паролей (bcrypt)
- JWT токены для аутентификации
- API ключи для внешних интеграций
- Валидация всех входящих данных
- Защита от SQL-инъекций и XSS

## 📚 Документация

После запуска приложения документация API доступна по адресам:

- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

## 🤝 Разработка

### Структура проекта (трехслойная архитектура)

```
app/
├── api/              # Presentation Layer - API роутеры
│   ├── auth.py
│   ├── users.py
│   ├── projects.py
│   ├── leads.py
│   └── external.py
├── services/         # Business Logic Layer - бизнес-логика
│   ├── user_service.py
│   ├── project_service.py
│   └── lead_service.py
├── repositories/     # Data Access Layer - доступ к данным
│   ├── user_repository.py
│   ├── project_repository.py
│   └── lead_repository.py
├── models.py         # Модели SQLAlchemy
├── schemas.py        # Pydantic схемы
├── auth.py           # Аутентификация и авторизация
├── config.py         # Настройки
├── database.py       # Настройка БД
├── tasks.py          # Celery задачи
├── notifications.py  # Уведомления
├── cli.py            # CLI команды для запуска
└── main.py           # Основное приложение
```

### Добавление новых функций

1. Создайте модель в `models.py`
2. Добавьте схему в `schemas.py`
3. Реализуйте репозиторий в `repositories/` (Data Access Layer)
4. Создайте сервис в `services/` (Business Logic Layer)
5. Создайте API роутер в `api/` (Presentation Layer)
6. Подключите роутер в `main.py`

**Архитектура:**
- **Repositories** - работа с базой данных (SQLAlchemy)
- **Services** - бизнес-логика, валидация, проверка прав доступа
- **API** - обработка HTTP запросов, валидация входных данных

## 📄 Лицензия

MIT License

## 🆘 Поддержка

При возникновении проблем:

1. Проверьте логи приложения
2. Убедитесь, что все зависимости установлены
3. Проверьте настройки базы данных и Redis
4. Создайте issue в репозитории проекта
